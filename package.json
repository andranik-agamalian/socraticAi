{
  "name": "socratic-ai",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "concurrently \"npm start --prefix server\" \"npm run dev --prefix client\"",
    "install:all": "npm install --prefix server && npm install --prefix client",
    "stop": "npm run stop-redis --prefix server"
  },
  "devDependencies": {
    "@types/multer": "^1.4.12",
    "concurrently": "^8.2.2"
  },
  "dependencies": {
    "@emotion/react": "^11.14.0",
    "@emotion/styled": "^11.14.0",
    "@mui/icons-material": "^6.4.2",
    "@mui/material": "^6.4.1",
    "dotenv": "^16.4.7",
    "multer": "^1.4.5-lts.1",
    "react-icons": "^5.4.0"
  }
}
